internal static partial class FrameScopeNativeMonitor
{
    // Live page implementation is split across PageLive.Layout, PageLive.Lifecycle,
    // PageLive.Log, and FrameScopeLiveData.Csv to keep visual structure, refresh
    // lifecycle, UI log actions, and data parsing independently editable.
}
