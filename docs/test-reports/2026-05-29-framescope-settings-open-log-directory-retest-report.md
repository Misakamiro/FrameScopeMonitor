# FrameScope Settings 打开日志目录复测报告

结论：PASS

复测时间：2026-05-29 22:30-22:33（Asia/Hong_Kong）

源码根目录：

`C:\Users\misakamiro\Documents\Codex\2026-05-02\files-mentioned-by-the-user-69eea97ef2a174a3524ae99fc416034d`

证据目录：

`artifacts\settings-open-log-directory-retest-20260529`

## 范围边界

- 只测试，不改源码逻辑。
- 未测试 BF6。
- 未启动真实游戏。
- 未安装本机应用。
- 未推 GitHub / Release。
- `build.ps1` 按现有脚本生成了 `dist\FrameScopeMonitor-Setup.exe` 和 `dist\FrameScopeMonitor-Full-Setup.exe`，本次仅把它们视为构建验证副作用，未执行安装。

## 复测结论

1. Settings -> 日志与诊断存在“打开日志目录”按钮：PASS。
   - 前端契约测试 `src\uiInteractionContract.test.ts` 覆盖 `data-smoke-action="open-logs-directory"`。
   - WebView2 smoke 截图覆盖 Settings 页面日志与诊断区域。
   - 截图证据：
     - `artifacts\settings-open-log-directory-retest-20260529\webview2-live-smoke-settings-sampling.png`
     - `artifacts\settings-open-log-directory-retest-20260529\webview2-reduced-motion-smoke-settings-sampling.png`

2. 点击后打开真实 FrameScope 日志目录：PASS。
   - live smoke 与 reduced-motion smoke 均验证 `logs.openDirectory` 空 payload 返回成功。
   - 真实日志目录检查：
     - `artifacts\settings-open-log-directory-retest-20260529\real-log-directory-check.json`
   - 当前 host 解析出的日志目录为源码根目录：
     - `C:\Users\misakamiro\Documents\Codex\2026-05-02\files-mentioned-by-the-user-69eea97ef2a174a3524ae99fc416034d`
   - 当前 watcher 日志存在：
     - `framescope-watcher.log`

3. 日志目录由 host 解析，不接受前端任意 path：PASS。
   - C# 测试 `FrameScopeWebBridgeTests.exe` PASS，覆盖 `logs.openDirectory` 拒绝前端 path payload。
   - WebView2 live smoke：`logsOpenPathRejected=true`。
   - WebView2 reduced-motion smoke：`logsOpenPathRejected=true`。
   - 汇总证据：
     - `artifacts\settings-open-log-directory-retest-20260529\webview2-smoke-summary.json`

4. 日志目录不存在时可以创建或返回清晰错误：PASS。
   - C# 测试 `FrameScopeWebBridgeTests.exe` PASS，覆盖缺失目录创建：
     - `LogsOpenDirectoryCreatesMissingLogDirectory`
   - 当前真实目录存在且 watcher 日志文件存在，见 `real-log-directory-check.json`。

5. 默认日志、详细日志、性能诊断日志、自动诊断文件策略不回退：PASS。
   - `FrameScopeConfigStoreTests.exe` PASS，覆盖默认值、保存合并后日志字段保留。
   - `FrameScopeDiagnosticsTests.exe` PASS，覆盖诊断配置归一化、诊断报告生成、清理策略。
   - `FrameScopeLoggingPolicyTests.exe` PASS，覆盖默认不启用 verbose/performance 噪音、健康 full report 不自动诊断、diagnostic/failed 才自动诊断。
   - 当前配置检查：
     - `EnableVerboseLogs=false`
     - `EnablePerformanceDiagnosticsLogs=false`
     - `AutoGenerateDiagnosticReport=false`
     - `LogRetentionDays=14`
     - `MaxLogDiskMb=100`
   - 证据：
     - `artifacts\settings-open-log-directory-retest-20260529\current-logging-policy-config.json`

## 命令结果

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| `tools\Run-Frontend.ps1 verify` | PASS | `frontend-verify.log` |
| `build.ps1` | PASS | `build.log` |
| `tests\Build-FrameScopeTests.ps1` | PASS | `build-tests.log` |
| `FrameScopeWebBridgeTests.exe` | PASS | `csharp-FrameScopeWebBridgeTests.log` |
| `FrameScopeConfigStoreTests.exe` | PASS | `csharp-FrameScopeConfigStoreTests.log` |
| `FrameScopeDiagnosticsTests.exe` | PASS | `csharp-FrameScopeDiagnosticsTests.log` |
| `FrameScopeLoggingPolicyTests.exe` | PASS | `csharp-FrameScopeLoggingPolicyTests.log` |
| `FrameScopeWebView2RuntimeTests.exe` | PASS | `csharp-FrameScopeWebView2RuntimeTests.log` |
| `FrameScopeWebHostLifecycleTests.exe` | PASS | `csharp-FrameScopeWebHostLifecycleTests.log` |
| WebView2 live smoke | PASS | `webview2-live-smoke.json`, `webview2-live-smoke.png` |
| WebView2 reduced-motion smoke | PASS | `webview2-reduced-motion-smoke.json`, `webview2-reduced-motion-smoke.png` |
| `git diff --check` | PASS | `git-diff-check-before-report.log` |
| 残留进程检查 | PASS | `residual-process-check-before-report.json` |

前端 verify 结果：

- TypeScript typecheck PASS。
- Vitest PASS：5 个测试文件，56 个测试。
- Vite production build PASS。

WebView2 smoke 汇总：

- live：`success=true`，`bridgeExtensionSuccess=true`，`logsOpenPathRejected=true`，`logsOpenDirectoryOk=true`。
- reduced-motion：`success=true`，`bridgeExtensionSuccess=true`，`logsOpenPathRejected=true`，`logsOpenDirectoryOk=true`。
- 两轮 smoke 均完成 `monitor.start` / `monitor.stop` 的测试内启停，并在结束后无残留 FrameScope/PresentMon/指定游戏进程。

`git diff --check` 说明：

- 未发现 whitespace error。
- 输出包含已有工作区 LF/CRLF warning，本次不处理换行风格，也不修改既有源码。

## 截图证据

- Settings 日志与诊断 live：
  - `artifacts\settings-open-log-directory-retest-20260529\webview2-live-smoke-settings-sampling.png`
- Settings 日志与诊断 reduced-motion：
  - `artifacts\settings-open-log-directory-retest-20260529\webview2-reduced-motion-smoke-settings-sampling.png`
- live smoke 最终截图：
  - `artifacts\settings-open-log-directory-retest-20260529\webview2-live-smoke.png`
- reduced-motion smoke 最终截图：
  - `artifacts\settings-open-log-directory-retest-20260529\webview2-reduced-motion-smoke.png`

## 未覆盖项

- 未做安装包安装后的本机已安装应用点击验证。
- 未验证 GitHub Release 资产。
- 未启动 BF6 或任何真实游戏。

## 是否建议进入本机安装更新验证

建议进入本机安装更新验证：是。

理由：源码树构建、前端契约、相关 C# 测试、WebView2 live/reduced smoke、bridge 安全边界、真实日志目录解析、日志策略与残留进程检查均已 PASS。下一步应在单独安装验证窗口确认安装后应用的 Settings “打开日志目录”点击行为与已安装路径一致；本次复测按边界未执行安装。
